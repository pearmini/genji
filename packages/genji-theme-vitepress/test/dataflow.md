# Dataflow

```js eval
add(a, b);
```

```js eval
function add(a, b) {
  return a + b;
}
```

```js eval
a = new Promise((resolve) => setTimeout(() => resolve(1), 1000));
```

```js eval
b = new Promise((resolve) => setTimeout(() => resolve(2), 500));
```
